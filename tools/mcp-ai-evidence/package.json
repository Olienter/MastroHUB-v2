{
  "name": "mcp-ai-evidence",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/server.js",
  "bin": {
    "mcp-ai-evidence": "dist/server.js"
  },
  "engines": { "node": ">=18.17" },
  "scripts": {
    "dev": "tsx src/server.ts",
    "build": "tsup src/server.ts --format esm --dts --sourcemap --out-dir dist --clean",
    "start": "node dist/server.js",
    "check": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.2.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "tsup": "^7.2.0",
    "tsx": "^4.16.0",
    "typescript": "^5.5.4"
  }
}
